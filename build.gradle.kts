/*
 * ══════════════════════════════════════════════════════════════════════════
 *  Oliver-200 · корневой build-скрипт.
 *  Плагины здесь НЕ объявляются намеренно: если положить Kotlin-плагин на
 *  общий classpath отсюда, модуль :app не сможет запросить kotlin("android")
 *  со своей версией, а объявить здесь AGP нельзя — тогда даже сборка одного
 *  :core потребует Android SDK.
 *  Подпись: OLIVER-200 · см. SIGNATURES.txt
 * ══════════════════════════════════════════════════════════════════════════
 */

tasks.register("printModules") {
    val names = subprojects.map { it.path }
    doLast { println("Модули: " + names.joinToString(", ")) }
}
